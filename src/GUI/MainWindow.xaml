<Window x:Class="GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="Interpreter" Height="675" Width="1400">
    <DockPanel>
        <!-- Top Menu -->
        <Menu DockPanel.Dock="Top" Background="White">
            <MenuItem Header="_Help">
                <MenuItem x:Name="HelpTutorialButton" Header="Tutorial" Click="HelpTutorialButton_Click" />
            </MenuItem>
        </Menu>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Input Group -->
            <GroupBox Header="Code" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5">
                <DockPanel Margin="5">
                    <DockPanel DockPanel.Dock="Bottom" 
                   Margin="0,5,0,0">
                        <Button x:Name="RunButton" 
                            DockPanel.Dock="Right"
                            Content="Run" 
                            Width="80"
                            Height="26"
                            Click="RunButton_Click" 
                            IsEnabled="False" />
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="OpenButton" 
                        Content="Open" 
                        Width="80"
                        Height="26"
                        Margin="0,0,5,0"
                        Click="OpenButton_Click" />
                            <Button x:Name="SaveButton" 
                        Content="Save" 
                        Width="80"
                        Height="26"
                        Click="SaveButton_Click"
                        IsEnabled="False" />
                        </StackPanel>
                    </DockPanel>
                    <TextBox x:Name="ExprTextBox" 
                         Text=""
                         TextChanged="ExprTextBox_TextChanged" 
                         AcceptsReturn="True" 
                         AcceptsTab="True"
                         ScrollViewer.VerticalScrollBarVisibility="Auto" FontSize="16" FontFamily="Cascadia Code Light" />
                </DockPanel>
            </GroupBox>

            <!-- Output + Plot Table -->
            <GroupBox Header="Output / Plot Table" Grid.Row="1" Grid.Column="0" Margin="0,0,5,0">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Output Text -->
                    <TextBox x:Name="OutputTextBox"
                             Grid.Column="0"
                             Margin="0,0,2.5,0"
                             Text=""
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             IsReadOnly="True" FontSize="16" FontFamily="Cascadia Code Light" />

                    <!-- Plot Table -->
                    <TextBox x:Name="PlottingTextBox"
                             Grid.Column="1"
                             Margin="2.5,0,0,0"
                             Text=""
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             IsReadOnly="True" FontSize="16" FontFamily="Cascadia Code Light" />
                </Grid>
            </GroupBox>

            <!-- Plot View -->
            <GroupBox Header="Plot" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Margin="0">
                <oxy:PlotView x:Name="PlotView" Margin="5"/>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>